import { Fraunces, Barlow } from 'next/font/google'

export const fraunces = Fraunces({ subsets: ['latin'] })
export const barlow_regular = Barlow({ weight: '400', subsets: ['latin'] })
export const barlow_bold = Barlow({ weight: '600', subsets: ['latin'] })
